with Ada.Text_IO
use Ada.Text_IO

procedure MyProg is

    Name : String (1 .. 5);

begin
    Put_Line("What is your name?");
    Get(Name);

    if ( Name = "Apple" ) then
        Put_Line("Hallo" & Name);
    else
        Put_Line("I don't know you.");
    end if;
end MyProg;