with Ada.Text_IO
use Ada.Text_IO

procedure MyProg is

    Name : String (1 .. 5);
    -- Este es un comentario
begin
    Put_Line("What is your name?");
    Get(Name);
    
    2+3*5/4-3*2

    if ( Name = "Apple" ) then
        Put_Line("Hallo" & Name & 's');
    else
        Put_Line("I don't know you.");
    end if;
end MyProg;